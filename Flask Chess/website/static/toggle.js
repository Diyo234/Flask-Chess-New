var colours = ["human", "computer"];

colours = colours.map( player => player === "human" ? "computer" : "human");

console.log(colours);
colours = colours.map( player => player === "human" ? "computer" : "human");
console.log(colours)